Run a full TypeScript check and production build for Forkable.

Inside `platform/`, run `npm run build`. Report:
- Whether TypeScript passed or failed (and what errors if any)
- Total pages generated and their routes
- Any warnings about missing env vars or unoptimized images
- Build time

If the build fails, diagnose the root cause and suggest a fix.
