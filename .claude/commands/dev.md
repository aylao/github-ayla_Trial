Start the Forkable Next.js dev server and confirm it's running.

Run `npm run dev` inside the `platform/` directory. Report the local URL and confirm the app loaded without errors. If there's a port conflict, note it.
